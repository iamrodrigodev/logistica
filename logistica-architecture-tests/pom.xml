<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>pe.com.mcalderon</groupId>
        <artifactId>logistica</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>logistica-architecture-tests</artifactId>

    <name>logistica-architecture-tests</name>
    <description>Pruebas transversales de arquitectura</description>

    <dependencies>

        <!-- =====================================================
             Módulos productivos
             ===================================================== -->

        <dependency>
            <groupId>pe.com.mcalderon</groupId>
            <artifactId>logistica-domain</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pe.com.mcalderon</groupId>
            <artifactId>logistica-application-read</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pe.com.mcalderon</groupId>
            <artifactId>logistica-application-write</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pe.com.mcalderon</groupId>
            <artifactId>logistica-read</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pe.com.mcalderon</groupId>
            <artifactId>logistica-write</artifactId>
            <version>${project.version}</version>
        </dependency>


        <!-- =====================================================
             Testing
             ===================================================== -->

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.4.2</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>

